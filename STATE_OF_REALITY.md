# STATE OF REALITY — Atmosphere Core / StratosAgent

> **Internal honesty document.** This is the *verified* state of the system as of
> 2026-05-31, produced by a direct end-to-end audit on the live VPS by Claude Code
> (Principal Systems QA). Where it conflicts with `walkthrough.md`, `task.md`, or the
> Antigravity "Phase N … 100% PASSED" reports, **this document is the source of truth.**
> Most of those reports describe *mock/simulated* verifications presented as production.
>
> Legend: ✅ **WORKING** (real, verified) · 🟡 **PARTIAL** (real code, degraded/not wired) · ⛔ **STUB/MOCK** (simulated; presented as done but not real)
>
> **Progress log:** 2026-05-31 — #1 RAG-hallucination **FIXED**; #2 real vector DB + embeddings **DONE** (nomic-embed-text 768-dim, all 507 rows migrated, relevance-gated); #3 **real PQC DONE** (`@noble/post-quantum` ML-DSA-65 + ML-KEM-768, FIPS 203/204 — no more mock); #4 P2P **transport proven REAL** (hyperdht Noise connect + data) and a real bootstrap bug fixed — full cross-machine mesh still needs a 2nd device + the firewalled DHT UDP port opened. **Night Shift GSI compiler** is now a **complete self-evolution pipeline** (harvest → classify → dedupe → compile → **full-module** PQC seal → verify-before-execute via `SkillExecutor`); signature now covers code **and** manifest (the prior integrity gap is closed), and tampered skills are refused before execution. **Tier A program synthesis** now auto-derives deterministic computation specs (const/affine/quadratic) from observed examples — the agent no longer hand-supplies the formula for that class. **The full self-evolution loop is now WIRED INTO and ENABLED on the live api-shim daemon** (OBSERVE→LEARN→EXECUTE). Behind hard flag gates that default OFF (a reload changes nothing until a flag is flipped); on this VPS the three flags are now set, the daemon booted clean (`🧬 engine online (observe=on, execute=on)`, `🌙 night-shift LEARN scheduler started`, listening :4099, restart count stable), and a real round-trip is verified — "What is double of 8?" → qwen "16", captured live to the production store as `auto_4aed46eee216` (1 example; correctly won't compile until a 2nd distinct input arrives). Three correctness fixes landed with the wiring: cognitive-skill writes are now **upsert-by-id** (no duplicate rows), captured examples **accumulate** under one skill id, and the inducer **refuses to synthesize from a single observation** (≥2 distinct inputs required) so one chat reply can't mint a constant-returning skill.
>
> **Progress log (later 2026-05-31) — the mesh became REAL; the agent gained self-awareness, memory, and a pipeline runtime (all Codex-Pattern-C-reviewed before code):**
> • **Real cross-machine P2P mesh.** Public Hyperswarm DHT + NAT hole-punch (no firewall change), PQC-signed skill gossip, verified live across the operator's OWN hardware (VPS + 16-core Ryzen mini-desktop + 4-core N150 laptop). Per-platform self-installing **node bundles** (win/mac/linux × x64/arm64) and **keyless relay bundles**. **Proof-of-capacity** challenge (a node cannot inflate its claimed compute — origin times the round-trip). A **distributed parallel job scheduler** (fan-out across the fleet — proven). **Multi-machine HA**: keyless relays + leader-election failover — killing the key-holder origin, a keyless relay (no private key) takes over and the worker keeps computing (proven live). The "needs a 2nd device" caveat below is RESOLVED.
> • **Identity layer.** The agent now introduces itself as **StratosAgent** with its real capabilities + zero-ambient-authority permission model (was a generic "I am a personal assistant"). Removed the **fabricated** `/balance` ("0.0084 SOL") and `/status` ("5 Nodes Online") Telegram readouts.
> • **Memory (MemGPT-lite, Codex-reviewed).** Tier 0 fixed an acute bug — every conversation was silently capped at Ollama's 2048-token default despite qwen2.5:7b's 32k — plus a bounded whole-exchange-block window planner. **Telegram per-chat memory**: the agent now remembers the conversation across turns (durable append-only ring, `/forget`).
> • **Folder-stage pipeline engine** (ICM "folders over agents"): workflows as numbered plain-text `stage.md` stages with human-editable intermediates + hash-based freshness/invalidation. 12/12 engine tests.
> • **PaymentEngine** barrel fixed (it exported the wrong 70-LOC class; now exposes the real 239-LOC settlement engine). **brain/** Antigravity hype docs quarantined to `docs/archive/` (gitignored). **reasoning-bank.js** confirmed USED (not "unused") — doc corrected. **Codex MCP** reconnected (full-path registration + re-login).
>
> **Progress log (2026-06-05) — the TRUST SUBSTRATE: built + hermetically tested, on a stacked feature branch, NOT yet reloaded onto the live daemon.** Status legend below applies; these are 🟡 by the strict "live on the running daemon" bar (the daemon still runs the prior policy until a Codex-review→merge + `pm2 reload`), but they are real code with tests that fail if the feature breaks (53/53 hermetic CI, up from 41). Built this session (each its own commit, linear stack tipped by `feat/mesh-signal`):
> • **Trust trifecta (capability gate · attribution ledger · identity broker), wired through the `SkillExecutor`.** *Capability gate* — signed skills carry least-privilege capabilities **inside the PQC-sealed manifest** (compute/actions/net/fs/secrets; absent ⇒ denied); the GSI compiler stamps minimal caps via `deriveCapabilities()`, the executor enforces deny-by-default before a skill runs (anti path-traversal proven: `/data/skills-evil` ⊄ `/data/skills`). *Attribution ledger* — append-only **tamper-evident hash chain** recording every verified run, attributed to this node's `did:atmos`; `summarize()` is measured units per contributor, **explicitly NOT a payout** (measurement before rewards, per the Vision/Architecture/Claim discipline). *Identity broker* — IDJAG/auth.md-style: mints **short-lived, audience-bound, scoped** assertions and returns ONLY the token, never the raw credential; deny-by-default (no grant / scope-beyond / undeclared-audience ⇒ refused). Wired live: every verified run is recorded + credentialed steps get a brokered token; `self-evolution.js` constructs the executor with `enforceCapabilities:true` + the ledger + `contributorId`. Tests: gate 14, enforcement 8, ledger 6, broker 9, trifecta-live 9.
> • **One sovereign model router, consolidated into the live request path.** `TaskClassifierRouter.classify()` no longer runs a second divergent policy — it delegates to one `model-router.js`. This **fixed a real sovereignty BUG**: the old fallback *"default to Cloud to ensure maximum intelligence"* sent general prompts to frontier APIs by default (and `evaluateComplexityScore≥5 → cloud` even with **no API key configured**, a call that could not have succeeded). Now: **LOCAL is the default; `/private` pins local; cloud is opt-in only** (a configured BYOK key on a genuinely hard prompt, or `/force-cloud`). A *named* cloud model from an OpenAI-compatible client no longer forces cloud (clients send a model on every call). `task-router.js` 185→80 lines (dead complexity/RAG-probe code removed; the LanceDB probe that made it non-hermetic is gone). Tests: model-router 7, classify-live 8, task-router 4-case sovereign spec, compliance-router 11.
> • **Real, file-backed mesh signal.** The router can route heavy work to the mesh, but only if it's actually there: `mesh-signal.js` reads the SAME self-reported `fleet.json` the CLI surfaces — deny-by-default, **never invents peers**. Returns **false today** (no persistent `fleet.json` exists), which is the honest current state; flips true when a live mesh node writes a real fleet with nodes>0+cores>0. Wired into `classify()` and `stratos route`. Tests: 8.
> • **Observability surface (`stratos id · ledger · route`) + ICM contract + Gemma 4 tier.** `stratos id whoami` derives this node's real `did:atmos:b672…5e45` from the on-disk node key; `ledger summary|verify|list` reads the persisted chain (verify fails code-1 on any tamper); `route <prompt>` previews the sovereign decision. `stratos icm init|validate` scaffolds the 5-layer "folders over agents" workspace. Gemma 4 added to the local-model ladder (preferred-when-installed, fallback-safe; `gemma4` is **not pulled on this box**, so it correctly falls through to `qwen2.5:7b`). Tests: id+ledger 14, route 11, icm 6, cli 33, model-manager 22.
> *Honest bar:* none of this is live on the running bridge yet — it's a reviewed-pending feature stack. "Live on the daemon" requires the operator's merge + `pm2 reload atmos-secure-bridge`.

---

## One-paragraph truth
You have a **real, running, self-aware local-first agent** plus a **genuinely real P2P compute
mesh** — both verified, not simulated. The bridge receives Telegram messages, answers with a
**real local `qwen2.5:7b`** (Ollama), now **knows it is StratosAgent**, **remembers each chat
across turns**, and manages its context window honestly. The sovereignty/crypto claims that used
to be mock are now **real**: hybrid post-quantum crypto (`@noble/post-quantum`, FIPS 203/204),
real LanceDB + semantic embeddings, and a real self-evolution loop (for the deterministic
numeric-transform class). The **mesh is real and cross-machine**: public Hyperswarm DHT +
hole-punch, PQC-signed skill gossip, node + keyless-relay bundles, proof-of-capacity, a working
parallel job scheduler, and multi-machine HA failover — all proven on the operator's own hardware.
There is also a folder-stage pipeline engine (ICM). **What remains genuinely NOT real:** the
entire **economic layer** (Solana token, on-chain settlement, DePIN reward harvesting), the
**multimodal** voice/vision, the **omni-channel** adapters, the ACP/DID/SD-JWT/Z3 **specs**, and
the **"superintelligence"** framing. So: a real sovereign agent + a real verified compute mesh,
with the economy and the AGI narrative still aspirational. Lead with what's real.

---

## ✅ WORKING — verified real this audit
| Component | Evidence |
| :-- | :-- |
| PM2 daemon `atmos-secure-bridge` (:4099) | online, polling, survived reloads |
| Telegram inbound bridge | real phone messages ("Yo","Test") received + routed in live logs |
| **Local inference (Ollama `qwen2.5:7b`)** | real HTTP 200 completion ("PONG"), real model output |
| Task classifier/router (local vs cloud) | real heuristic logic; correct decisions verified |
| Telegram HTML / `<tg-spoiler>` formatting | fixed + unit-proven; no more "can't parse entities" |
| Ollama model-alias fix (no 404→mock) | verified live; bridge now serves the real model |
| ffmpeg `.oga` voice transcode fix | corrected (regex matched only `.ogg`) |
| x402 `PaymentEngine` **off-chain logic** (PoW + state-channel + settlement math) | proved 5000/5000 mined-PoW invoices, exact lamport accounting, zero double-spend |
| VaultHost memory hygiene (`.fill(0)`) | passcode/salt/key/seed wiped correctly |
| BSL 1.1 fork licenses | all four preserve `Copyright (c) Holepunch` + filled params |
| Secrets vault (`.secrets-vault/env_blueprint.md`, 600) + `vault-set.sh` | live-read by bridge; rotation flow working |
| git auth via `gh` credential helper | `git ls-remote` works with no token in URL |
| **Agent identity / self-awareness** | `identity.js` injected into every route; live "who are you?" → introduces as StratosAgent + real capabilities + permission model. Fabricated `/balance`/`/status` readouts removed. |
| **Conversation memory** | MemGPT-lite Tier 0 (num_ctx 2048→8192 fix + whole-block window planner, 9/9 invariant tests, live "Teal"); Telegram per-chat append-only memory (live "Rufus" recall, 9/9 tests, `/forget`) |
| **Real cross-machine P2P mesh** | public DHT + hole-punch + PQC-signed skill gossip proven VPS↔devices; node+relay bundles (6 arches); 2-relay leader-election **HA failover proven live** (kill key-holder → keyless relay takes over, worker keeps computing) |
| **Proof-of-capacity + job scheduler** | challenge–response (origin times round-trip → node can't inflate compute); parallel job fan-out across the fleet proven (40 inputs split, results aggregated) |
| **Folder-stage pipeline engine** | ICM "folders over agents"; 12/12 engine tests (hash-freshness, human-edit + downstream invalidation, failure→blocked, path-traversal reject); live `defaultModelRunner` confirmed |
| **PaymentEngine barrel** | now exports the real 239-LOC settlement engine (was the 70-LOC stub); invoice signer renamed `X402InvoiceEngine`; all 3 payment test suites green |
| **Mesh node-identity signatures (`KeyringManager`)** | ✅ **FIXED 2026-05-31** — was a **non-cryptographic placeholder**: `sign()` was a keyed SHA-256 and `verify()` returned true for ANY 32-byte signature with ANY 32-byte pubkey (it verified **nothing** — any peer could forge any `P2PNetwork` node card / x402 invoice). Now **real Ed25519** detached signatures (Node WebCrypto, 64-byte sigs) with fail-closed verification; same interface, so P2P cards + x402 + telemetry keep working but are now genuinely authenticated. 16 unit checks incl. *the exact old bypass (forged sig + forged pubkey) is now rejected*; all 4 atmos-core integration tests still green. *Honest scope:* classical Ed25519 *transport identity* (proves key possession, not authZ — key **pinning/trust-anchoring is still open**); the skill-execution gate already used the real hybrid Ed25519+ML-DSA-65 seal (`quantum-crypto.js`). A PQC (ML-DSA) upgrade of the node card itself is a tracked follow-up. Surfaced by a Codex Pattern-C review of the sovereign-compute design. |
| **Configurable agent + chat setup shortcuts** | Authoritative two-tier config (`agent-config.js`: `agent-config.json` user prefs + separate `runtime-state.json` for owner binding), revision-guarded + **cross-process file-locked** writes, one-time `.env.local` migration; `identity.js`/`stratos-ctl` now read it (chat/CLI rename takes effect everywhere). **Owner+DM-gated** natural-language *setup shortcuts* (`config-intents.js`): set name, switch among **local** models, read capabilities — privileged grants (files/network/shell) and **cloud-provider switches are CLI-only** (chat explains, never grants); negation/quote/hypothetical guarded. **Bridge `secret-guard`** refuses key-shaped messages (13 token shapes incl. contextual `*_secret=…`) on the RAW message *before* normalization/log/persistence/model — on text AND voice transcripts. `stratos-ctl bind <chat-id>` sets the owner. Design + **two** Codex Pattern-C reviews (design, then implementation: 4 findings — 2 HIGH/1 MED/1 LOW — all fixed). 75 unit checks (secret-guard 25, agent-config 25, config-intents 25). *Honest scope:* deterministic phrase grammar (marketed as "setup shortcuts," not open-ended NL config); LLM tool-calling config is a flagged v2. |

## 🟡 PARTIAL — real code, but degraded / incomplete / not wired
| Component | Reality |
| :-- | :-- |
| Cloud route | classifier decides "cloud" correctly, but `:5001` upstream is a **local-Ollama-backed stand-in** I built — not a real frontier cloud. Local fallback enabled. |
| Context isolation (`isolatedContextTag`) | I added the LanceDB-tag filter + threading and proved it blocks cross-channel bleed; full omni-gateway wiring still partial |
| WASI sandbox | structurally safe (preview1 = no sockets/shell; deny-by-default preopens); I hardened env passthrough + network gate. **Not executing real compiled skills in the live flow** — Test 5 is happy-path only |
| ~~RAG retrieval~~ | ✅ **FIXED 2026-05-31** — triviality gate (no RAG on greetings/short prompts) + relevance gate (`RAG_RELEVANCE_MAX_DISTANCE=0.95`) + reframed prompt. No more hallucinated codebase replies. |
| x402 payments | engine logic real & stress-proven, but Solana settlement is **offline-signed only, never broadcast.** No real wallet movement, no devnet/mainnet tx |
| `verify-proxy-flow.js` | rewritten to pass; needs isolated stores (contends with live daemon otherwise) |

## ⛔ STUB / MOCK — simulated; reported as done but not real
| Component | Reality |
| :-- | :-- |
| ~~Post-quantum crypto (ML-DSA-65 / ML-KEM-768)~~ | ✅ **NOW REAL 2026-05-31** — `@noble/post-quantum` (audited pure-JS FIPS 203/204) replaces the mock in `quantum-crypto.js` + `vault-host.js`. Real hybrid X25519+ML-KEM-768 KEM and Ed25519+ML-DSA-65 signatures (1952-byte pubkey, 3309-byte sig). chaos-pqc detects real tampers; verified key round-trips. (No Node upgrade needed; native ML-KEM only exists in Node ≥24.7.) |
| ~~LanceDB vector store~~ | ✅ **CORRECTED/WORKING** — the main chat RAG path (`vector-bank.js`) uses **real LanceDB** with a real on-disk store. **Correction (2026-05-31 runtime check):** `reasoning-bank.js` is NOT "unused" — it imports cleanly and IS used by `server.js`'s `knowledge-base` endpoints (`vectorInsert`/`vectorSearch`, lines ~191/674), is re-exported by `stratos-agent/index.js`, and is a required dependency of `gsi-scheduler.js` (throws without it). It is a *separate* path from the chat RAG and runs in a degraded `[ReasoningBank (LanceDB Sim)]` mode (`better-sqlite3` ABI). So: kept (referenced in 3 places), not deleted; the prior "unused" label was wrong. |
| ~~Embeddings~~ (`generateEmbedding`) | ✅ **NOW REAL 2026-05-31** — swapped the `sin(charCode)` fake for **`nomic-embed-text`** (768-dim, unit-normalized) via local Ollama. Proven semantic (related 0.81 vs unrelated −0.07); all 507 rows re-embedded; concept queries return the correct source files. |
| ~~P2P mesh~~ (Hyperswarm) | ✅ **NOW REAL + CROSS-MACHINE 2026-05-31 (later).** Resolved the "needs a 2nd device" caveat: the mesh runs on the **public Hyperswarm DHT with NAT hole-punching** (no firewall change needed — peers connect without open ports), with **PQC-signed skill gossip** (a node runs a skill only if its ML-DSA-65 + Ed25519 seal verifies against the pinned origin key). Proven live VPS↔operator devices (16-core Ryzen + N150). Ships self-installing **node** + **keyless-relay** bundles per platform. **Multi-machine HA proven**: 2 relays with lowest-live-id leader election — kill the key-holder origin, a *keyless* relay (no private key) promotes and the worker keeps computing. Plus **proof-of-capacity** (anti-inflation) and a **parallel job scheduler** (fan-out + aggregate). The durable origin runs as a PM2 service. *Honest limit:* capacity is self-reported until a public-mesh proof-of-capacity challenge gates strangers; the operator's fleet is the verified set today. |
| **chaos-mesh / chaos-cognitive** | **SIM.** Virtual peers + mock offload to fake nodes (`127.0.0.1:5001-5003`). Not real network/failure testing. |
| ~~"Night Shift" GSI compiler~~ | ✅ **REAL self-evolution pipeline 2026-05-31** — the full automation loop runs and is proven end-to-end (`test-nightshift-e2e`): **harvest** real `success_rate=1.0` skills from LanceDB → **distill/classify** (computational vs automation, `TraceAnalyzer.distill`) → **content-hash dedupe** (unchanged skills skip recompile; persisted `dist/skills/registry.json`) → **compile** to wasm → **full-module PQC seal** → write to mesh dist. A `SkillExecutor` then **verifies-before-running**: computational skills *really execute* (`3·10+7 = 37`); automation skills are signed 3-step replayable manifests; tampered skills are **refused before execution**. **Gap CLOSED:** the ML-DSA-65 + Ed25519 signature now covers the **entire module — executable code bytes AND manifest** (flipping a code byte now fails verification; previously only the manifest was signed). The scheduler (`GsiScheduler`) + evolution worker (`NightShiftCompiler`) both delegate to this one engine. **Caveat now PARTIALLY CLOSED (Tier A inducer, 2026-05-31):** for the **deterministic class** (const / affine / quadratic) the computation spec is now **auto-synthesized from observed input→output examples** — the agent no longer hand-supplies `{a,b}`. `SkillInductionEngine` (`skill-induction.js`) does exact program synthesis (Occam-ordered, integer-coefficient, accepted only if it reproduces *every* example; non-deterministic examples → honest `null`, never a guess), and the night-shift classifier auto-induces from any `examples`-bearing trace (proven: `(0,7)(1,10)(4,19)→3x+7`, `x²−2x+1`, induced→compiled→signed→`compute(10)=73`). **Still a frontier (Tier B):** arbitrary algorithms with control flow/loops — planned path is LLM-propose-a-program → verify against trace examples in the sandbox → `egg`/`Stitch` library-learning to compound primitives (research-mapped, not yet built). Real browser execution of automation manifests needs a Playwright/harness driver bound to `SkillExecutor.actionExecutor` (interface is real; default is an honest verified dry-run). **WIRED LIVE 2026-05-31 (flag-gated, default OFF):** `SelfEvolutionEngine` is now connected to the api-shim daemon via one seam (`self-evolution-runtime.js`) — Hook B (LEARN, night-shift scheduler on boot), Hook A (OBSERVE, captures typed numeric I/O from successful chat exchanges), Hook E (EXECUTE, serves a verified wasm skill instead of the ~100 s LLM). With no `STRATOS_EVOLUTION*` env var set, all three are inert and the daemon is byte-identical to before; proven end-to-end by `packages/api-shim/test-evolution-seam.mjs` (12/12: inert-when-off → OBSERVE-accumulate → induce `3x` → EXECUTE a differently-worded "triple 9" = 27 through the verify gate). **Honest scope:** the live chat path learns/serves only the deterministic **numeric-transform class** (prompt with an integer operand → integer answer); free-form prose carries no typed examples, so OBSERVE records nothing and EXECUTE never matches it — by design, not stub. **P2P DISTRIBUTE/VERIFY is built + unit-proven in the engine but deliberately NOT wired into the daemon** — the firewalled VPS has no second peer, so live gossip would be untested; it activates once a real second node exists. |
| **Multimodal** (Whisper STT, TTS, Active Vision) | **MOCK.** GDI mock display buffer, mock transcription. Voice confirmed "1990s robot," scrapped. |
| **Omni-channel adapters** (Slack/Discord/WhatsApp) | **SCAFFOLD.** Not connected to live platforms. |
| **ACP / MCP gateway, DIDs, SD-JWT Verifiable Intent, Z3 SMT** | **SPEC ONLY** (PRD-level). Not implemented in running code. |
| **Solana token, on-chain settlement, peer compute harvesting** | **NOT REAL.** The peer compute "service" is a no-op background loop; no real compute is harvested or paid. |
| **"Triple-layer superintelligence / federated LoRA training"** | **CONCEPTUAL.** Not implemented. |

---

## Infrastructure reality
- **Compute:** VPS is **CPU-only** (4 vCPU / 16 GB, no GPU) → ~100 s per reply. The mesh is now a **real, verified multi-node fleet** — VPS (origin, PM2 service) + 16-core Ryzen mini-desktop + 4-core N150 laptop, PQC-verified, with proof-of-capacity + a working job scheduler. *Not* a "global supercompute mesh" yet (3 of the operator's own machines; the real `node` bundle replaces the old no-op loop, but a large public fleet is future).
- **"Cloud" tier / Universal Model Manager:** ✅ **BUILT + tested** (2026-06-08 verify). BYOK to OpenAI/Gemini/Anthropic natively + **OpenRouter** (one key → 100+ models via `vendor/model` slugs); real `fetch()` to the official endpoints (`api-shim/src/routers/cloud-byok.js` + `anthropic-adapter.js`), raw-body pass-through, keys read from env/vault and **never logged**. Sovereign: LOCAL is default; cloud only on opt-in (`/force-cloud` or a genuinely hard prompt) **AND** a configured BYOK key, behind the **402 cost-approval gate**. Tests: `test-model-manager` (22) + `test-compliance-gateway` (25) pass. Without a key it falls back to local **by design (not a stub)**; a real end-to-end frontier call requires the user's BYOK key. *(This line previously said "unimplemented" — that was stale.)*

## Recommended build order (make things *real*, one at a time)
1. ~~**Fix the RAG-injection hallucination**~~ ✅ **DONE 2026-05-31.**
2. ~~**Real vector DB + real embeddings**~~ ✅ **DONE 2026-05-31** (nomic-embed-text, 768-dim, real LanceDB confirmed). LanceDB was already real; only the embedder was fake.
3. ~~**Decide PQC honestly**~~ ✅ **DONE 2026-05-31** — made it genuinely real via `@noble/post-quantum` (ML-DSA-65 + ML-KEM-768, FIPS 203/204). No Node upgrade / ABI risk.
4. ~~**One real P2P link**~~ ✅ **DONE 2026-05-31 (later)** — real cross-machine mesh on the public DHT (hole-punch, no firewall change), PQC-signed gossip, node+relay bundles, proof-of-capacity, job scheduler, and multi-machine HA failover, all proven on the operator's hardware. "Mesh in progress" → "mesh real (operator fleet)".
5. ~~**Agent identity + honest readouts**~~ ✅ **DONE.** ~~**MemGPT-lite memory (Tier 0) + Telegram per-chat memory**~~ ✅ **DONE.** ~~**Folder-stage pipeline engine**~~ ✅ **DONE.**
6. **Next (real, buildable):** Universal Model Manager (BYOK + local open-weights — clean path only, no subscription scraping); MemGPT Tier 1 Part 2 (async evict→LanceDB recall); mesh-dispatched pipeline stage; public-mesh proof-of-capacity gate.
7. **Defer / freeze** (need product-truth + legal counsel — see `docs/GROUNDED_STRATEGY.md` §6): token, on-chain payments/settlement, DePIN reward harvesting, headless subscription scraping, frontier-output training, AGI/superintelligence framing.

*Build claims should be backed by a test that fails if the feature breaks. Today, most "passing" tests pass because the thing they test is mocked.*
